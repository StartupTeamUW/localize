-- Drops the blogger if it exists currently --
DROP DATABASE IF EXISTS localizeUser_db;
-- Creates the "myblog" database --
CREATE DATABASE localizeUser_db;

-- Drops the blogger if it exists currently --
DROP DATABASE IF EXISTS localizeTrip_db;
-- Creates the "myblog" database --
CREATE DATABASE localizeTrip_db;
